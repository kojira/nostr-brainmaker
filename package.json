{
  "name": "nostr-brainmaker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Nostr 脳内メーカー — visualize a Nostr author's last 7 days of posts as a brain-maker-style image.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:live": "vitest run --config vitest.live.config.js",
    "collect": "node scripts/collect.js",
    "label": "node scripts/label.js",
    "pipeline": "node scripts/pipeline.js",
    "report:labels": "node scripts/report-labels.js"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "franc": "^6.2.0",
    "nostr-tools": "^2.10.4"
  },
  "devDependencies": {
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
